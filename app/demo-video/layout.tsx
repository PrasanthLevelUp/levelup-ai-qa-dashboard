import './animations.css';

export const metadata = {
  title: 'LevelUp AI QA — Self-Healing Test Automation',
  description: 'Watch how LevelUp AI QA heals broken tests automatically, saving 99% on costs with a 94% success rate.',
};

export default function DemoVideoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
