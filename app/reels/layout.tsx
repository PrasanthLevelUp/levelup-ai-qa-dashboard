import './reel-animations.css';

export const metadata = {
  title: 'LevelUp AI QA — Reels',
  description: 'Short-form marketing reels for LevelUp AI QA features.',
};

export default function ReelsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
